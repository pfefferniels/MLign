# MLign — STATE (compaction-proof summary)

Budget: RED / share 9.1% (tier low) / HALT (as of 2026-08-16T22:00+02:00) — week 22% used at 11% elapsed; resets Sat 2026-08-23 01:59 Berlin. Promo (+50%) ends Aug 19 → capacity drops ~⅓ after; don't plan heavy phases for Aug 20+ on today's headroom.
Rule (verwaltung/Niels): run `credits budget --agent mlign --json` at the start of every cycle; RUN=normal; THROTTLE=no new expensive fronts, defer discretionary phases, fan-out ≤3; HALT=finish in-flight, rewrite STATE.md, journal, wake ≥3600s, do NOT start new phases. As `low` I resume only at GREEN. Disagreement with the ordering → PENDING-APPROVAL.md, never self-change the tier.

## PARKED (HALT) — where I stopped, 2026-08-16 22:00

Nothing costly in flight. Two local niced dev-long evals (v6real2 snap-e024/e025)
were running at halt time; they finish on their own (~30 min) and write
eval/results/devlong-v6e02{4,5}.json — zero budget cost, no action needed.

## Mission status — DELIVERED

- **HEADLINE (banked, confirmed):** `models/mlign-v1.pt` (v5real epoch 21) beats
  DualDTW on the untouched nASAP holdout: **0.9878 vs 0.9852** (84 perfs, 65W/2T/17L,
  sign test p<1e-7), best on ins (.945) and del (.877); e23 confirms (.9874).
  TheGlueNote .9778. Also: Bar C folded-score .992 pooled (symbolic-only first),
  Batik parity-or-better, Bar B tie.
- **PUBLIC:** https://github.com/pfefferniels/MLign (MIT). README + docs/RESULTS.md
  (generated by scripts/make_results.py) + docs/DESIGN.md + LOG.md (full journal).
- **Product:** CLI `mlign align` (json/match/jsonl); browser test page
  `mlign serve` → http://127.0.0.1:8765/ (web/index.html; demo pair bundled).
  Server may still be running locally for Niels (pkill -f mlign.serve to stop).

## Deferred (resume at GREEN, in this order)

1. **v6real2 holdout candidates:** dev-long e023 = **0.9573 (best ever; v3 .9516)** —
   the representative-val run improved the long-sonata regime (Beethoven was the one
   composer group still −.002 vs DualDTW). NEXT ACTION when allowed: read
   devlong-v6e024/025.json, pick the dev-long best of e023-e025, run holdout:
   `eval/run_eval.py --aligner model:runs/v6real2/snap-eNNN.pt --robust-only --split test --out eval/results/v6real2-eNNN-robust-test.json`
   (bar .9878 = current mlign-v1). If it wins → promote to models/mlign-v1.pt
   (or v1.1), regenerate docs/RESULTS.md, commit+push. ~50 min local eval, cheap.
2. Matchability-aware leftover decode (src/mlign/infer.py) — the structural ins/del
   lever (null-bias sweep proved thresholds don't move it).
3. Portability: make ESPRESSIVO_DIST an env var / auto-detect so MEI upload works
   off this machine (README caveat documents it now).
4. `--format mei` CLI export via src/mlign/mei_export.py.
5. Cluster (bridge agent, socket rotates — ListAgents; last uds:/tmp/cc-socks/29262.sock):
   idle, all corpora synced, workspace to 2026-10-09. Next training idea when
   budget allows: v7 = v5 recipe (full realgt-train 6,284) + realgt2-val selection.

## Peers / promises

- mpmify (uds 16120, session was down at send time): owed the milestone relay + the
  three-way table for their write-up; their real-data front-end = mlign-v1 via
  `mlign align --format jsonl`.
- meico-ts (ornamentation + exaggeration campaigns) merged upstream, integrated;
  nothing owed. bwUniCluster bridge: standing invitation, nothing queued.
- stgall agent routed to the bridge for cluster access.

## Key facts a fresh context needs

- Eval protocol: eval/metrics.py (TISMIR match-F, unit-tested); headline set =
  nASAP robust ∩ MAESTRO-v2 test (eval/split.py, 84 perfs); dev tiers: 4x22
  (eval/run_4x22.py, NOT representative of long sonatas) and dev-long
  (eval/run_devlong.py, calibrated to rank like the holdout).
- Selection lesson: mixed synthetic val AND 4x22 both pick wrong checkpoints;
  real-music val (realgt-val / realgt2-val) picks right. Never trust a dev
  record without dev-long + holdout.
- M1 rules: no local training; single niced evals; memory/m1-training-constraints.md.
